We analyze US gun murder data collected by the FBI
download-data.R- downolads csvfile to data directory 
wrangle-data.R-creaes a derived dataset and saves as R object
analysis.R generates a barplot and saves it to the figs directory 